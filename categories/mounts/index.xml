<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Mounts on UAPI Group Kernel Feature Wishlist</title><link>https://uapi-group.org/kernel-features/categories/mounts/</link><description>Recent content in Mounts on UAPI Group Kernel Feature Wishlist</description><generator>Hugo</generator><language>en-us</language><atom:link href="https://uapi-group.org/kernel-features/categories/mounts/index.xml" rel="self" type="application/rss+xml"/><item><title>Create empty mount namespaces via `unshare(UNSHARE_EMPTY_MNTNS)` and `clone3(CLONE_EMPTY_MNTNS)`</title><link>https://uapi-group.org/kernel-features/in-progress/create-empty-mount-namespaces-via-unshare-unshare-empty-mntns-and-clone3-clone-empty-mntns/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/create-empty-mount-namespaces-via-unshare-unshare-empty-mntns-and-clone3-clone-empty-mntns/</guid><description>&lt;p>Now that we have support for &lt;code>nullfs&lt;/code> it is trivial to allow the
creation of completely empty mount namespaces, i.e., mount namespaces
that only have the &lt;code>nullfs&lt;/code> mount located at it&amp;rsquo;s root.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> This allows to isolate tasks in completely empty mount
namespaces. It also allows the caller to avoid copying its current mount
table which is useless in the majority of container workload cases.&lt;/p></description></item><item><title>Ability to put user xattrs on `S_IFSOCK` socket entrypoint inodes in the file system</title><link>https://uapi-group.org/kernel-features/in-progress/ability-to-put-user-xattrs-on-s-ifsock-socket-entrypoint-inodes-in-the-file-system/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/ability-to-put-user-xattrs-on-s-ifsock-socket-entrypoint-inodes-in-the-file-system/</guid><description>&lt;p>Currently, the kernel only allows extended attributes in the
&lt;code>user.*&lt;/code> namespace to be attached to directory and regular file
inodes. It would be tremendously useful to allow them to be
associated with socket inodes, too.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> There are two syslog RFCs in use today: RFC3164 and
RFC5424. &lt;code>glibc&lt;/code>&amp;rsquo;s &lt;code>syslog()&lt;/code> API generates events close to the
former, but there are programs which would like to generate the
latter instead (as it supports structured logging). The two formats
are not backwards compatible: a client sending RFC5424 messages to a
server only understanding RFC3164 will cause an ugly mess. On Linux
there&amp;rsquo;s only a single &lt;code>/dev/log&lt;/code> AF_UNIX/SOCK_DGRAM socket backing
&lt;code>syslog()&lt;/code>, which is used in a one-way, fire-and-forget style. This
means that feature negotation is not really possible within the
protocol. Various tools bind mount the socket inode into &lt;code>chroot()&lt;/code>
and container environments, hence it would be fantastic to associate
supported feature information directly with the inode (and thus
outside of the protocol) to make it easy for clients to determine
which features are spoken on a socket, in a way that survives bind
mounts. Implementation idea would be that syslog daemons
implementing RFC5425 could simply set an xattr &lt;code>user.rfc5424&lt;/code> to &lt;code>1&lt;/code>
(or something like that) on the socket inode, and clearly inform
clients in a natural and simple way that they&amp;rsquo;d be happy to parse
the newer format. Also see:
&lt;a href="https://github.com/systemd/systemd/issues/19251">https://github.com/systemd/systemd/issues/19251&lt;/a> – This idea could
also be extended to other sockets and other protocols: by setting
some extended attribute on a socket inodes, services could advertise
which protocols they support on them. For example D-Bus sockets
could carry &lt;code>user.dbus&lt;/code> set to &lt;code>1&lt;/code>, and Varlink sockets
&lt;code>user.varlink&lt;/code> set to &lt;code>1&lt;/code> and so on.&lt;/p></description></item><item><title>Support detached mounts with `pivot_root()`</title><link>https://uapi-group.org/kernel-features/in-progress/support-detached-mounts-with-pivot-root/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/support-detached-mounts-with-pivot-root/</guid><description>&lt;p>The new rootfs must currently refer to an attached mount. This restriction
seems unnecessary. We should allow the new rootfs to refer to a detached
mount.&lt;/p>
&lt;p>This will allow a service- or container manager to create a new rootfs as
a detached, private mount that isn&amp;rsquo;t exposed anywhere in the filesystem and
then &lt;code>pivot_root()&lt;/code> into it.&lt;/p>
&lt;p>Since &lt;code>pivot_root()&lt;/code> only takes path arguments the new rootfs would need to
be passed via &lt;code>/proc/&amp;lt;pid&amp;gt;/fd/&amp;lt;nr&amp;gt;&lt;/code>. In the long run we should add a new
&lt;code>pivot_root()&lt;/code> syscall operating on file descriptors instead of paths.&lt;/p></description></item><item><title>Create mount namespace with custom rootfs via `open_tree()` and `fsmount()`</title><link>https://uapi-group.org/kernel-features/in-progress/create-mount-namespace-with-custom-rootfs-via-open-tree-and-fsmount/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/create-mount-namespace-with-custom-rootfs-via-open-tree-and-fsmount/</guid><description>&lt;p>Add &lt;code>OPEN_TREE_NAMESPACE&lt;/code> flag to &lt;code>open_tree()&lt;/code> and &lt;code>FSMOUNT_NAMESPACE&lt;/code> flag
to &lt;code>fsmount()&lt;/code> that create a new mount namespace with the specified mount tree
as the rootfs mounted on top of a copy of the real rootfs. These return a
namespace file descriptor instead of a mount file descriptor.&lt;/p>
&lt;p>This allows &lt;code>OPEN_TREE_NAMESPACE&lt;/code> to function as a combined
&lt;code>unshare(CLONE_NEWNS)&lt;/code> and &lt;code>pivot_root()&lt;/code>.&lt;/p>
&lt;p>When creating containers the setup usually involves using &lt;code>CLONE_NEWNS&lt;/code> via
&lt;code>clone3()&lt;/code> or &lt;code>unshare()&lt;/code>. This copies the caller&amp;rsquo;s complete mount namespace.
The runtime will also assemble a new rootfs and then use &lt;code>pivot_root()&lt;/code> to
switch the old mount tree with the new rootfs. Afterward it will recursively
unmount the old mount tree thereby getting rid of all mounts.&lt;/p></description></item><item><title>Add immutable rootfs (`nullfs`)</title><link>https://uapi-group.org/kernel-features/in-progress/add-immutable-rootfs-nullfs/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/add-immutable-rootfs-nullfs/</guid><description>&lt;p>Currently &lt;code>pivot_root()&lt;/code> doesn&amp;rsquo;t work on the real rootfs because it
cannot be unmounted. Userspace has to do a recursive removal of the
initramfs contents manually before continuing the boot.&lt;/p>
&lt;p>Add an immutable rootfs called &lt;code>nullfs&lt;/code> that serves as the parent mount
for anything that is actually useful such as the tmpfs or ramfs for
initramfs unpacking or the rootfs itself. The kernel mounts a
tmpfs/ramfs on top of it, unpacks the initramfs and fires up userspace
which mounts the rootfs and can then simply do:&lt;/p></description></item><item><title>Allow creating idmapped mounts from idmapped mounts</title><link>https://uapi-group.org/kernel-features/completed/allow-creating-idmapped-mounts-from-idmapped-mounts/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/allow-creating-idmapped-mounts-from-idmapped-mounts/</guid><description>&lt;p>[x] Allow creating idmapped mounts from idmapped mounts&lt;/p>
&lt;p>Add a new &lt;code>OPEN_TREE_CLEAR&lt;/code> flag to &lt;code>open_tree()&lt;/code> that can only be
used in conjunction with &lt;code>OPEN_TREE_CLONE&lt;/code>. When specified it will clear
all mount properties from that mount including the mount&amp;rsquo;s idmapping.
Requires the caller to be &lt;code>ns_capable(mntns-&amp;gt;user_ns)&lt;/code>. If idmapped mounts
are encountered the caller must be &lt;code>ns_capable(sb-&amp;gt;user_ns, CAP_SYS_ADMIN)&lt;/code>
in the filesystems user namespace.&lt;/p>
&lt;p>Locked mount properties cannot be changed. A mount&amp;rsquo;s idmapping becomes
locked if it propagates across user namespaces.&lt;/p></description></item><item><title>Ability to only open regular files</title><link>https://uapi-group.org/kernel-features/wishlist/ability-to-only-open-regular-files/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/ability-to-only-open-regular-files/</guid><description>&lt;p>&lt;code>O_REGULAR&lt;/code> (inspired by the existing &lt;code>O_DIRECTORY&lt;/code> flag for
&lt;code>open()&lt;/code>), which opens a file only if it is of type &lt;code>S_IFREG&lt;/code>.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> this would be very useful to write secure programs
that want to avoid being tricked into opening device nodes with
special semantics while thinking they operate on regular
files. This is particularly relevant as many device nodes (or even
FIFOs) come with blocking I/O (or even blocking &lt;code>open()&lt;/code>!) by
default, which is not expected from regular files backed by &amp;ldquo;fast&amp;rdquo;
disk I/O. Consider implementation of a naive web browser which is
pointed to &lt;code>file://dev/zero&lt;/code>, not expecting an endless amount of
data to read.&lt;/p></description></item><item><title>Allow `MOVE_MOUNT_BENEATH` on the rootfs</title><link>https://uapi-group.org/kernel-features/in-progress/allow-move-mount-beneath-on-the-rootfs/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/allow-move-mount-beneath-on-the-rootfs/</guid><description>&lt;p>Allow &lt;code>MOVE_MOUNT_BENEATH&lt;/code> to target the caller&amp;rsquo;s rootfs, enabling
root-switching without &lt;code>pivot_root(2)&lt;/code>. The traditional approach to
switching the rootfs involves &lt;code>pivot_root(2)&lt;/code> or a &lt;code>chroot_fs_refs()&lt;/code>-based
mechanism that atomically updates &lt;code>fs-&amp;gt;root&lt;/code> for all tasks sharing the
same &lt;code>fs_struct&lt;/code>. This has consequences for &lt;code>fork()&lt;/code>, &lt;code>unshare(CLONE_FS)&lt;/code>,
and &lt;code>setns()&lt;/code>.&lt;/p>
&lt;p>Instead, decompose root-switching into individually atomic, locally-scoped
steps:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-c" data-lang="c">&lt;span style="display:flex;">&lt;span>fd_tree &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#a6e22e">open_tree&lt;/span>(&lt;span style="color:#f92672">-&lt;/span>EBADF, &lt;span style="color:#e6db74">&amp;#34;/newroot&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> OPEN_TREE_CLONE &lt;span style="color:#f92672">|&lt;/span> OPEN_TREE_CLOEXEC);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#a6e22e">fchdir&lt;/span>(fd_tree);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#a6e22e">move_mount&lt;/span>(fd_tree, &lt;span style="color:#e6db74">&amp;#34;&amp;#34;&lt;/span>, AT_FDCWD, &lt;span style="color:#e6db74">&amp;#34;/&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> MOVE_MOUNT_BENEATH &lt;span style="color:#f92672">|&lt;/span> MOVE_MOUNT_F_EMPTY_PATH);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#a6e22e">chroot&lt;/span>(&lt;span style="color:#e6db74">&amp;#34;.&amp;#34;&lt;/span>);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#a6e22e">umount2&lt;/span>(&lt;span style="color:#e6db74">&amp;#34;.&amp;#34;&lt;/span>, MNT_DETACH);&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Since each step only modifies the caller&amp;rsquo;s own state, the
&lt;code>fork()&lt;/code>/&lt;code>unshare()&lt;/code>/&lt;code>setns()&lt;/code> races are eliminated by design.&lt;/p></description></item><item><title>Require a user namespace to have an idmapping when attached</title><link>https://uapi-group.org/kernel-features/completed/require-a-user-namespace-to-have-an-idmapping-when-attached/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/require-a-user-namespace-to-have-an-idmapping-when-attached/</guid><description>&lt;p>[x] Require a user namespace to have an idmapping when attached&lt;/p>
&lt;p>Enforce that the user namespace about to be attached to a mount must
have an idmapping written.&lt;/p>
&lt;p>&lt;strong>🙇 &lt;code>dacfd001eaf2 (&amp;quot;fs/mnt_idmapping.c: Return -EINVAL when no map is written&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Tighten the semantics.&lt;/p></description></item><item><title>Mount notifications without rescanning of `/proc/self/mountinfo`</title><link>https://uapi-group.org/kernel-features/completed/mount-notifications-without-rescanning-of-proc-self-mountinfo/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/mount-notifications-without-rescanning-of-proc-self-mountinfo/</guid><description>&lt;p>[x] Mount notifications without rescanning of &lt;code>/proc/self/mountinfo&lt;/code>&lt;/p>
&lt;p>Mount notifications that do not require continuous rescanning of
&lt;code>/proc/self/mountinfo&lt;/code>. Currently, if a program wants to track
mounts established on the system it can receive &lt;code>poll()&lt;/code>able
events via a file descriptor to &lt;code>/proc/self/mountinfo&lt;/code>. When
receiving them it needs to rescan the file from the top and
compare it with the previous scan. This is both slow and
racy. It&amp;rsquo;s slow on systems with a large number of mounts as the
cost for re-scanning the table has to be paid for every change to
the mount table. It&amp;rsquo;s racy because quickly added and removed
mounts might not be noticed.&lt;/p></description></item><item><title>Query mount information via file descriptor with `statmount()`</title><link>https://uapi-group.org/kernel-features/in-progress/query-mount-information-via-file-descriptor-with-statmount/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/in-progress/query-mount-information-via-file-descriptor-with-statmount/</guid><description>&lt;p>Extend &lt;code>struct mnt_id_req&lt;/code> to accept a file descriptor and introduce
&lt;code>STATMOUNT_BY_FD&lt;/code> flag. When a valid fd is provided and &lt;code>STATMOUNT_BY_FD&lt;/code>
is set, &lt;code>statmount()&lt;/code> returns mount info about the mount the fd is on.&lt;/p>
&lt;p>This works even for &amp;ldquo;unmounted&amp;rdquo; mounts (mounts that have been unmounted using
&lt;code>umount2(mnt, MNT_DETACH)&lt;/code>), if you have access to a file descriptor on that
mount. These unmounted mounts will have no mountpoint and no valid mount
namespace, so &lt;code>STATMOUNT_MNT_POINT&lt;/code> and &lt;code>STATMOUNT_MNT_NS_ID&lt;/code> are unset in
&lt;code>statmount.mask&lt;/code> for such mounts.&lt;/p></description></item><item><title>Determining if a mount point belongs to the current user</title><link>https://uapi-group.org/kernel-features/wishlist/determining-if-a-mount-point-belongs-to-the-current-user/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/determining-if-a-mount-point-belongs-to-the-current-user/</guid><description>&lt;p>Ability to determine if a mount point belongs to the current user
namespace, in order to check if there&amp;rsquo;s a chance a process can
safely unmount it (as that only works for mounts owned by the same
user namespaces — or one further down the tree, but not any up the
tree). A simple, additional field in &lt;code>/proc/self/mountinfo&lt;/code>
container the owning user namespace ID would probably already
suffice.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> the &lt;code>systemd&lt;/code> system and service manager tries to unmount
all established mounts on shutdown. Inside of container
environments where specific mounts are established by the
container manager (and not the payload itself) this will
ultimately fail if user namespaces are enabled. In order to clean
up the shutdown logic it would be very good to be able to
determine whether a specific mount could even possibly be
unmounted or whether it&amp;rsquo;s not worth the effort to include the
unmount in the system shutdown transaction.&lt;/p></description></item><item><title>Mount a subdirectory instead of the top-level directory</title><link>https://uapi-group.org/kernel-features/completed/mount-a-subdirectory-instead-of-the-top-level-directory/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/mount-a-subdirectory-instead-of-the-top-level-directory/</guid><description>&lt;p>[x] Mount a subdirectory instead of the top-level directory&lt;/p>
&lt;p>Ability to mount a subdirectory of a regular file system instead of
the top-level directory. E.e. for a file system &lt;code>/dev/sda1&lt;/code> which
contains a sub-directory &lt;code>/foobar&lt;/code> mount &lt;code>/foobar&lt;/code> without having
to mount its parent directory first. Consider something like this:&lt;/p>
&lt;pre tabindex="0">&lt;code>mount -t ext4 /dev/sda1 somedir/ -o subdir=/foobar&lt;/code>&lt;/pre>&lt;p>(This is of course already possible via some mount namespacing
shenanigans, but this requires namespacing to be available, and is
not precisely obvious to implement. Explicit kernel support at mount
time would be much preferable.)&lt;/p></description></item><item><title>Read-only propagation of mounts</title><link>https://uapi-group.org/kernel-features/wishlist/read-only-propagation-of-mounts/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/read-only-propagation-of-mounts/</guid><description>&lt;p>A way to mark mounts that receive mount propagation events from
elsewhere so that these propagated mounts are established
read-only implicitly. Right now, if a mount receives a mount
propagation event it will have the exact same &lt;code>MS_RDONLY&lt;/code>,
&lt;code>MS_NODEV&lt;/code>, … flags as it has where it originated. It would be
very useful if an &lt;code>MS_RDONLY&lt;/code> could be ORed into the mount flags
automatically whenever propagated elsewhere.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> various mount namespace based sandboxes
(e.g. &lt;code>systemd&lt;/code>&amp;rsquo;s &lt;code>ProtectSystem=&lt;/code> option) mark large parts of the
host file hierarchy read-only via mounting it
&lt;code>MS_RDONLY|MS_BIND|MS_REMOUNT&lt;/code>, but generally intend to leave the
file hierarchy besides that the way it is, and that includes they
typically still want to be able to receive mount events to
directories such as &lt;code>/mnt/&lt;/code> and &lt;code>/media/&lt;/code> in these sandboxed
environments. Right now, any such propagation then happens in
writable mode, even if the file hierarchy otherwise is almost
entirely read-only. To close this gap it would be great if such
propagated mounts could implicitly gain &lt;code>MS_RDONLY&lt;/code> as they are
propagated.&lt;/p></description></item><item><title>Unmounting of obstructed mounts</title><link>https://uapi-group.org/kernel-features/completed/unmounting-of-obstructed-mounts/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/unmounting-of-obstructed-mounts/</guid><description>&lt;p>[x] ability to unmount obstructed mounts. (this means: you have a stack
of mounts on the very same inode, and you want to remove a mount in
the middle. right now, you can only remove the topmost mount.)&lt;/p>
&lt;p>&lt;strong>🙇 instead of the ability to unmount obstructured mounts we gained
the ability to mount beneath an existing mount, with mostly
equivalent outcome. &lt;code>6ac392815628f317fcfdca1a39df00b9cc4ebc8b (&amp;quot;fs: allow to mount beneath top mount&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p></description></item><item><title>A system call to query information about a mount</title><link>https://uapi-group.org/kernel-features/completed/a-system-call-to-query-information-about-a-mount/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/a-system-call-to-query-information-about-a-mount/</guid><description>&lt;p>[x] Implement a mount-specific companion to &lt;code>statx()&lt;/code> that puts at least the
following information into &lt;code>struct mount_info&lt;/code>:&lt;/p>
&lt;p>&lt;strong>🙇 &lt;code>46eae99ef73302f9fb3dddcd67c374b3dffe8fd6 (&amp;quot;add statmount(2) syscall&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>mount flags: &lt;code>MOUNT_ATTR_RDONLY&lt;/code>, &amp;hellip;&lt;/li>
&lt;li>time flags: &lt;code>MOUNT_ATTR_RELATIME&lt;/code>, &amp;hellip;
Could probably be combined with mount flags.&lt;/li>
&lt;li>propagation setting: &lt;code>MS_SHARED)&lt;/code>, &amp;hellip;&lt;/li>
&lt;li>peer group&lt;/li>
&lt;li>mnt id of the mount&lt;/li>
&lt;li>mnt id of the mount&amp;rsquo;s parent&lt;/li>
&lt;li>owning userns&lt;/li>
&lt;/ul>
&lt;p>There&amp;rsquo;s a bit more advanced stuff systemd would really want but which
I think is misplaced in a mountinfo system call including:&lt;/p></description></item><item><title>Asynchronous `close()`</title><link>https://uapi-group.org/kernel-features/wishlist/asynchronous-close/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/asynchronous-close/</guid><description>&lt;p>An asynchronous or forced &lt;code>close()&lt;/code>, that guarantees that
userspace doesn&amp;rsquo;t have to risk blocking for longer periods of time
when trying to get rid of unwanted file descriptors, possibly
received via &lt;code>recvmsg()&lt;/code> + &lt;code>SCM_RIGHTS&lt;/code> (see above). Currently,
&lt;code>close()&lt;/code> of various file descriptors (for example those referring
to slow storage, e.g. non-responding NFS servers and such) might
take arbitrary amounts of time, potentially into the minute range
and more. This makes it risky accepting file descriptors on
publicly accessible &lt;code>AF_UNIX&lt;/code> sockets, the way like IPC brokers
(e.g. D-Bus) do it: if a rogue client keeps sending file
descriptors that because unexpected must be closed immediately it
might cause the receiving process to effectively crawl, when it is
busy closing them all. A special form of &lt;code>close()&lt;/code> that simply
detaches a file descriptor from the file descriptor table without
blocking on IO in any form would be great to close this issue.&lt;/p></description></item><item><title>Idmapped mounts for tmpfs</title><link>https://uapi-group.org/kernel-features/completed/idmapped-mounts-for-tmpfs/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/idmapped-mounts-for-tmpfs/</guid><description>&lt;p>[x] Support idmapped mounts for tmpfs&lt;/p>
&lt;p>&lt;strong>🙇 &lt;code>7a80e5b8c6fa (&amp;quot;shmem: support idmapped mounts for tmpfs&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Runtimes such as Kubernetes use a lot of &lt;code>tmpfs&lt;/code> mounts of
individual files or directories to expose information to containers/pods.
Instead of having to change ownership permanently allow them to use an
idmapped mount instead.&lt;/p>
&lt;p>@rata and @giuseppe brought this suggestion forward. For Kubernetes it is
sufficient to support idmapped mounts of &lt;code>tmpfs&lt;/code> instances mounted in the
initial user namespace. However, in the future idmapped
mounts of &lt;code>tmpfs&lt;/code> instances mounted in user namespaces should be supported.
Other container runtimes want to make use of this. The kernel is able to
support this since at least &lt;code>5.17&lt;/code>.&lt;/p></description></item><item><title>Immutable loopback block devices</title><link>https://uapi-group.org/kernel-features/wishlist/immutable-loopback-block-devices/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/immutable-loopback-block-devices/</guid><description>&lt;p>Truly immutable loopback block devices. Right now setting up a
loopback block device in read-only mode, backed by a read-only
file (stored on a regular read/write file system), and then
mounting it with &lt;code>ext4&lt;/code> also in &lt;code>MS_RDONLY&lt;/code>mode &lt;em>will&lt;/em> result in
changes to the file, quite unexpectedly 🤯. Ideally, if a loopback
block device is set up in read-only mode this should guarantee
that the backing file remains unmodified by it.&lt;/p></description></item><item><title>Extend `mount_setattr()` to allow changing mount properties ignoring any failures</title><link>https://uapi-group.org/kernel-features/wishlist/extend-mount-setattr-to-allow-changing-mount-properties-ignoring-any-failures/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/extend-mount-setattr-to-allow-changing-mount-properties-ignoring-any-failures/</guid><description>&lt;p>&lt;strong>Use-Case:&lt;/strong> workloads that know that there are mounts in a mount tree
whose attributes cannot be changed by the caller don&amp;rsquo;t want
&lt;code>mount_settattr()&lt;/code> to fail on the first mount it failed to convert. Give
them a flag to request changes ignoring failures.&lt;/p></description></item><item><title>Namespace `binfmt_misc` filesystem</title><link>https://uapi-group.org/kernel-features/completed/namespace-binfmt-misc-filesystem/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/namespace-binfmt-misc-filesystem/</guid><description>&lt;p>[x] Make the &lt;code>binfmt_misc&lt;/code> filesystem namespaced.&lt;/p>
&lt;p>&lt;strong>🙇 &lt;code>21ca59b365c0 (&amp;quot;binfmt_misc: enable sandboxed mounts&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Allow containers and sandboxes to register their own binfmt
handlers.&lt;/p></description></item><item><title>Support idmapped mounts for `overlayfs`</title><link>https://uapi-group.org/kernel-features/completed/support-idmapped-mounts-for-overlayfs/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/completed/support-idmapped-mounts-for-overlayfs/</guid><description>&lt;p>[x] Support idmapped mounts for &lt;code>overlayfs&lt;/code>&lt;/p>
&lt;p>&lt;strong>🙇 &lt;code>bc70682a497c (&amp;quot;ovl: support idmapped layers&amp;quot;)&lt;/code> 🙇&lt;/strong>&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Allow containers to use &lt;code>overlayfs&lt;/code> with idmapped mounts.&lt;/p></description></item><item><title>Make quotas work with user namespaces</title><link>https://uapi-group.org/kernel-features/wishlist/make-quotas-work-with-user-namespaces/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/make-quotas-work-with-user-namespaces/</guid><description>&lt;p>The quota codepaths in the kernel currently broken and inconsistent
and most interesting operations are guarded behind
&lt;code>capable(CAP_SYS_ADMIN)&lt;/code>, i.e., require &lt;code>CAP_SYS_ADMIN&lt;/code> in the initial
user namespace. We should rework these codepaths to work with user
namespaces and then see whether we can make them work with idmapped
mounts.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> using quotas correctly in containers.&lt;/p></description></item><item><title>Add `MOUNT_SETATTR_LOCKED` flag to `mount_setattr()`</title><link>https://uapi-group.org/kernel-features/wishlist/add-mount-setattr-locked-flag-to-mount-setattr/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/add-mount-setattr-locked-flag-to-mount-setattr/</guid><description>&lt;p>Add a new &lt;code>MOUNT_SETATTR_LOCKED&lt;/code> flag to &lt;code>mount_setattr(..., ..., MOUNT_SETATTR_LOCKED, ..., ...)&lt;/code>.
The &lt;code>MOUNT_SETATTR_LOCKED&lt;/code> flag allow a &lt;code>ns_capable(mntns-&amp;gt;user_ns, CAP_SYS_ADMIN)&lt;/code> caller to lock all mount properties. The mount properties
cannot be changed anymore.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> allowing processes to lock mount properties even for
privileged processes. Locking mount properties would currently involve
having to have the mount namespace of the container be owned by an ancestor
user namespace. But this doesn&amp;rsquo;t just lock a single mount or mount subtree
it locks all mounts in the mount namespace, i.e., the mount table cannot be
altered.&lt;/p></description></item><item><title>Security hook for `mount_setattr()`</title><link>https://uapi-group.org/kernel-features/wishlist/security-hook-for-mount-setattr/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/security-hook-for-mount-setattr/</guid><description>&lt;p>(kAPI) Add security hook to &lt;code>mount_setattr()&lt;/code>.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Allow LSMs to make decisions about what mount properties to
allow and what to deny.&lt;/p></description></item><item><title>Extend `io_uring` with classic synchronous system calls</title><link>https://uapi-group.org/kernel-features/wishlist/extend-io-uring-with-classic-synchronous-system-calls/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/extend-io-uring-with-classic-synchronous-system-calls/</guid><description>&lt;p>The &lt;code>io_uring&lt;/code> subsystem is open to adding classic existing synchronous
system calls (e.g. &lt;code>setns()&lt;/code> or &lt;code>mount()&lt;/code> or other) to &lt;code>io_uring&lt;/code>.
They also said they would support adding new functionality into
&lt;code>io_uring&lt;/code> that is not exposed through system calls yet.&lt;/p></description></item><item><title>Map different uids/gids to the same uids/gids?</title><link>https://uapi-group.org/kernel-features/wishlist/map-different-uids-gids-to-the-same-uids-gids/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/map-different-uids-gids-to-the-same-uids-gids/</guid><description>&lt;p>Explore the idea of mapping different uids/gids to the same uids/gids, i.e.
65534:1000:1 50000:1000:1. This will only work if the mount is read-only as
the kernel wouldn&amp;rsquo;t know what uid/gid would need to be put to disk
otherwise (65534? 50000? the first one that is mapped?).&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Delegate multiple {g,u}ids to the same user. Merging
ownership similar to how overlayfs merges files. Bindfs
(&lt;a href="https://bindfs.org/docs/bindfs.1.html#sect3">https://bindfs.org/docs/bindfs.1.html#sect3&lt;/a>) allows this concept too.&lt;/p></description></item><item><title>Namespaced loop and block devices</title><link>https://uapi-group.org/kernel-features/wishlist/namespaced-loop-and-block-devices/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/namespaced-loop-and-block-devices/</guid><description>&lt;p>Namespace-able loop and block devices, usable inside user namespaces.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Allow mounting images inside nspawn containers, and using
RootImage= and friends in the systemd user manager.&lt;/p></description></item><item><title>Device cgroup guard to allow `mknod()` in non-initial userns</title><link>https://uapi-group.org/kernel-features/wishlist/device-cgroup-guard-to-allow-mknod-in-non-initial-userns/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/device-cgroup-guard-to-allow-mknod-in-non-initial-userns/</guid><description>&lt;p>If a container manager restricts its unprivileged (user namespaced)
children by a device cgroup, it is not necessary to deny &lt;code>mknod()&lt;/code>
anymore. Thus, user space applications may map devices on different
locations in the file system by using &lt;code>mknod()&lt;/code> inside the container.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> A use case for this, which is applied by users of GyroidOS,
is to run &lt;code>virsh&lt;/code> for VMs inside an unprivileged container. &lt;code>virsh&lt;/code> or
libvirt creates device nodes, e.g., &lt;code>/var/run/libvirt/qemu/11-fgfg.dev/null&lt;/code>
which currently fails in a non-initial userns, even if a cgroup device white
list with the corresponding major, minor of &lt;code>/dev/null&lt;/code> exists. Thus, in
this case the usual bind mounts or pre populated device nodes under &lt;code>/dev&lt;/code>
are not sufficient.&lt;/p></description></item><item><title>Race-free mounting of block devices</title><link>https://uapi-group.org/kernel-features/wishlist/race-free-mounting-of-block-devices/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/race-free-mounting-of-block-devices/</guid><description>&lt;p>Introduce a new struct to &lt;code>fsconfig()&lt;/code> as an alternative to the
&lt;code>source&lt;/code> property. The struct contains at least a pointer to a path,
possibly a device minor and major, and a diskseq number. The VFS can
expose a helper that filesystems can call and use the diskseq number
to verify that the block device they are intending to mount is indeed
the one they want to mount.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> Race-free mounting of block devices.&lt;/p></description></item><item><title>Ability to reopen a `struct block_device`</title><link>https://uapi-group.org/kernel-features/wishlist/ability-to-reopen-a-struct-block-device/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/ability-to-reopen-a-struct-block-device/</guid><description>&lt;p>Add ability to reopen a &lt;code>struct block_device&lt;/code>. This would allow using
&lt;code>blkdev_get_by_path()&lt;/code>/&lt;code>blkdev_get_{part,whole}()&lt;/code> to claim a device
with &lt;code>BLK_OPEN_READ&lt;/code> and later on reopen with
&lt;code>BLK_OPEN_READ | BLK_OPEN_WRITE&lt;/code>. This in turn would opening block
devices at &lt;code>fsconfig(FS_CONFIG_SET_*)&lt;/code> time and then at &lt;code>fill_super()&lt;/code>
time we would be able to reopen in case the &lt;code>!(fc-&amp;gt;sb_flags &amp;amp; SB_RDONLY)&lt;/code>.
Overall this has the effect that we&amp;rsquo;re able to open devices early
giving the user early errors when they set mount options rather than
very late when the superblock is created.&lt;/p></description></item><item><title>Path-based ACL management in an LSM hook</title><link>https://uapi-group.org/kernel-features/wishlist/path-based-acl-management-in-an-lsm-hook/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://uapi-group.org/kernel-features/wishlist/path-based-acl-management-in-an-lsm-hook/</guid><description>&lt;p>The LSM module API should have the ability to do path-based (not
just inode-based) ACL management.&lt;/p>
&lt;p>&lt;strong>Use-Case:&lt;/strong> This would be useful in BPF-LSM modules such as
systemd&amp;rsquo;s &lt;code>mntfsd&lt;/code> which allows unprivileged file system mounts in
some cases, and which would like to restrict ACL handling based on
the superblock involved.&lt;/p></description></item></channel></rss>